var si='this is a typeof string message'
var c= si.replace(/\s*/g,"")
console.log(c)
// RegExp
//g:全局匹配 ； i :忽略大小写 m:多行匹配 s :点号匹配所有字符  u(unicode):对待pattern作为Unicode代码点的序列 y(粘性，粘性匹配) 
// 仅匹配lastIndex目标字符串中此正则表达式的属性所指示的索引。不会尝试从任何更高版本的索引进行匹配。
//正则
